<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:task="http://www.springframework.org/schema/task"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.3.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.3.xsd">
	<!-- 在开发之中C3P0数据库连接池使用非常广泛 -->
	<bean id="dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
		<property name="driverClass" value="${database.driverClass}"/>	<!-- 驱动程序 -->
		<property name="jdbcUrl" value="${database.jdbcUrl}"/>	<!-- 连接地址 -->
		<property name="user" value="${database.user}"/>	<!-- 用户名 -->
		<property name="password" value="${database.password}"/>	<!-- 密码 -->
		<property name="maxPoolSize" value="${database.maxPoolSize}"/>	<!-- 最大连接数 -->
		<property name="initialPoolSize" value="${database.initialPoolSize}"/>	<!-- 初始化大小 -->
		<property name="minPoolSize" value="${database.minPoolSize}"/> 	<!-- 最小连接数 -->
		<property name="maxIdleTime" value="${database.maxIdleTime}"/>	<!-- 等待时间 -->
	</bean>	


	<!-- 配置JdbcTemplate模版的处理对象，该对象如果要想使用一定要配置数据库连接 -->
	<bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource" ref="dataSource"/>						<!-- 配置数据源 -->
	</bean>
	
</beans>
